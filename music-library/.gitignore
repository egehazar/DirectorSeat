venv/
__pycache__/
*.pyc
.DS_Store

# Audio files are large binaries — track the tags + database, not the source audio.
raw/**/*.mp3
raw/**/*.wav
raw/**/*.flac
raw/**/*.m4a
raw/**/*.ogg

# Allow .gitkeep so the empty raw/ folder shows up in git.
!raw/.gitkeep
!raw/**/.gitkeep
